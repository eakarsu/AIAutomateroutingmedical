import{Router}from'express';import{validateIncident,evaluateDispatch,assertTransition}from'../domain/dispatchWorkflow.js';const router=Router(),tenantFor=u=>String(u.tenant_id||u.agency_id||`legacy-user:${u.id}`);
router.get('/external-capabilities',(req,res)=>res.json({cad:{configured:false},avl:{configured:false},traffic:{configured:false},hospitalStatus:{configured:false},radio:{configured:false}}));
router.get('/',async(req,res)=>{try{const r=await req.app.locals.db.query('SELECT * FROM dispatch_cases WHERE tenant_id=$1 ORDER BY updated_at DESC',[tenantFor(req.user)]);res.json(r.rows);}catch{res.status(500).json({error:'Unable to load dispatch cases'});}});
router.post('/',async(req,res)=>{const key=req.get('Idempotency-Key');if(!key)return res.status(400).json({error:'Idempotency-Key header is required'});const validation=validateIncident(req.body||{});if(!validation.valid)return res.status(422).json(validation);const tenant=tenantFor(req.user),recommendation=evaluateDispatch(req.body);let client;try{client=await req.app.locals.db.connect();await client.query('BEGIN');const prior=await client.query('SELECT * FROM dispatch_cases WHERE tenant_id=$1 AND idempotency_key=$2',[tenant,key]);if(prior.rows[0]){await client.query('ROLLBACK');return res.json(prior.rows[0]);}const c=await client.query(`INSERT INTO dispatch_cases(tenant_id,idempotency_key,external_incident_id,priority,payload,recommendation,created_by)VALUES($1,$2,$3,$4,$5,$6,$7)RETURNING *`,[tenant,key,req.body.incident.externalId,req.body.incident.priority,req.body,recommendation,String(req.user.id)]);await client.query(`INSERT INTO dispatch_case_events(tenant_id,case_id,actor_id,event_type,to_status,event_data)VALUES($1,$2,$3,'created','received',$4)`,[tenant,c.rows[0].id,String(req.user.id),recommendation]);await client.query('COMMIT');res.status(201).json(c.rows[0]);}catch{if(client)await client.query('ROLLBACK');res.status(500).json({error:'Unable to create dispatch case'});}finally{if(client)client.release();}});
router.post('/:id/transition',async(req,res)=>{const{toStatus,expectedVersion,rationale,dispatchCommand}=req.body||{};if(!Number.isInteger(expectedVersion)||!rationale)return res.status(400).json({error:'expectedVersion and rationale are required'});const tenant=tenantFor(req.user);let client;try{client=await req.app.locals.db.connect();await client.query('BEGIN');const f=await client.query('SELECT * FROM dispatch_cases WHERE id=$1 AND tenant_id=$2 FOR UPDATE',[req.params.id,tenant]),current=f.rows[0];if(!current){await client.query('ROLLBACK');return res.status(404).json({error:'Dispatch case not found'});}assertTransition(current.status,toStatus,req.user,current,dispatchCommand);if(current.version!==expectedVersion){await client.query('ROLLBACK');return res.status(409).json({error:'Version conflict',currentVersion:current.version});}const u=await client.query(`UPDATE dispatch_cases SET status=$1,version=version+1,updated_at=NOW(),dispatched_by=CASE WHEN $1='dispatched' THEN $2 ELSE dispatched_by END WHERE id=$3 AND tenant_id=$4 AND version=$5 RETURNING *`,[toStatus,String(req.user.id),current.id,tenant,expectedVersion]);await client.query(`INSERT INTO dispatch_case_events(tenant_id,case_id,actor_id,event_type,from_status,to_status,event_data)VALUES($1,$2,$3,'transition',$4,$5,$6)`,[tenant,current.id,String(req.user.id),current.status,toStatus,{rationale,dispatchCommand}]);await client.query('COMMIT');res.json(u.rows[0]);}catch(e){if(client)await client.query('ROLLBACK');const bad=/not allowed|authority|required|dispatch command/.test(e.message);res.status(bad?422:500).json({error:bad?e.message:'Unable to transition dispatch case'});}finally{if(client)client.release();}});
router.get('/:id/history',async(req,res)=>{try{const r=await req.app.locals.db.query('SELECT * FROM dispatch_case_events WHERE case_id=$1 AND tenant_id=$2 ORDER BY occurred_at,id',[req.params.id,tenantFor(req.user)]);res.json(r.rows);}catch{res.status(500).json({error:'Unable to load history'});}});export default router;

