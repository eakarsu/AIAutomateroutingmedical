import test from'node:test';import assert from'node:assert/strict';import{validateIncident,evaluateDispatch,assertTransition}from'../domain/dispatchWorkflow.js';const now=Date.parse('2026-07-18T12:00:00Z'),input={incident:{externalId:'CAD-1',priority:1,location:{lat:40,lng:-74},requiredCapabilities:['als']},source:{system:'CAD',eventId:'evt-1',version:'3',receivedAt:'2026-07-18T11:59:00Z'},units:[{id:'U1',status:'available',capabilities:['als'],location:{lat:40.01,lng:-74},observedAt:'2026-07-18T11:59:00Z'}],hospitals:[{id:'H1',availableBeds:2,observedAt:'2026-07-18T11:59:00Z'}],traffic:{version:'v7',observedAt:'2026-07-18T11:59:00Z'},failSafe:{fallbackChannel:'radio',staleAfterMinutes:5}};test('validates source and fail-safe data',()=>assert.equal(validateIncident(input).valid,true));test('recommends only while inputs are current',()=>assert.equal(evaluateDispatch(input,now).recommendedUnitId,'U1'));test('stale traffic forces manual-only',()=>assert.equal(evaluateDispatch({...input,traffic:{version:'v6',observedAt:'2026-07-18T11:00:00Z'}},now).manualOnly,true));test('priority one requires supervisor',()=>assert.throws(()=>assertTransition('dispatcher_review','dispatched',{id:2,role:'dispatcher'},{created_by:1,priority:1},{cadCommandId:'C1',radioAcknowledged:true}),/supervisor/));

